#include <stdio.h>
#include <stdlib.h>

int main (void){
    int numeros,count;
    numeros = -10;
    while(numeros <= 10){
        printf("%d\n",numeros);
        numeros++;
        
    }
    return 0;
}